# Azure OpenAI Settings
AZURE_OPENAI_API_KEY=your-api-key-here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2025-01-01-preview
AZURE_OPENAI_DEPLOYMENT=gpt-4o-mini-0

# Anki Settings (requires Anki running with AnkiConnect plugin)
# Note: When using Docker Compose, ANKI_URL is automatically set to
# http://host.docker.internal:8765 to connect to host machine's Anki
ANKI_URL=http://127.0.0.1:8765
ANKI_DECK_NAME=Korean::Auto
ANKI_MODEL_NAME=Basic
ANKI_VOCAB_MODEL_NAME=Korean_Vocab_Auto

# Target Word List Settings (for coverage check feature)
# Directory containing your target word list files (default: data)
# WORD_LIST_DIR=data
# Default target word list file (default: korean_words.txt)
# DEFAULT_WORD_LIST=korean_words.txt

# CORS Configuration (comma-separated list of allowed origins)
# Used for frontend development. Default includes common dev server ports.
# CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://127.0.0.1:5173,http://127.0.0.1:5174
