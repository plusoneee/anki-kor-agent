# Docker Compose Wrapper
# This file provides a convenient way to run docker-compose from the root directory.
# The actual configuration is in deployment/docker-compose.yml

include:
  - deployment/docker-compose.yml
